// if (tracker == 1)
//     tracker = tracker + 1;

#include <bits/stdc++.h>
using namespace std;
int main()
{
  ios_base::sync_with_stdio(false);
  cin.tie(NULL);
  vector<int> arr;
  arr.push_back(2);
  arr.push_back(2);
  arr.push_back(3);
  // cout << arr[100];
  int som[] = {1, 2, 3, 5, 6, 7, 8, 99, 99};
  cout << som[10];
  // use .at to avoid bound errors

  return 0;
}