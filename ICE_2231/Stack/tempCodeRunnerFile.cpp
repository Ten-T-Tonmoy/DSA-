if (mode == STACK_MODE)
            {
                if (!s.empty())
                {
                    cout << "Popped from stack: " << s.top() << endl;
                    s.pop();
                }
            }